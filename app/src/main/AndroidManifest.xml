<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appdevproject">

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NumberRegisterActivity"></activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".GetStartActivity" />
        <activity android:name=".OthersReg"></activity>
        <activity android:name=".StudentReg" />
        <activity android:name=".AboutYou" />
        <activity android:name=".OTP" />
        <activity android:name=".secondAnime"/>
        <activity
            android:name=".InitialActivity"
            android:finishOnTaskLaunch="true" />
        <activity android:name=".Splash1Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>